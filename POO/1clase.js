//La POO es un paradigma(una forma de programar) que se basa en el concepto de "objetos", que son instancias de clases. Las clases son plantillas o moldes que definen las propiedades (atributos) y comportamientos (métodos) que los objetos pueden tener. La POO permite organizar el código de manera más modular, reutilizable y fácil de mantener.
// objetos son conjunto de datos 

const auto ={ marca: "Toyota", modelo: "Corolla",ruedas:"4", año: 2020, color: "Rojo"}
    arrancar()
    console.log (`${auto.marca}:arrancando`)
    //This es una palabra reservada que hace referencia al objeto actual, en este caso el auto
    //ejemplo de uso de this 
 
    